import { downloadJson } from '../utils/storage';
export default function Header({title,subtitle,data,setData,onSave}){function importJson(e){const f=e.target.files?.[0]; if(!f)return; const r=new FileReader(); r.onload=()=>{try{setData(JSON.parse(r.result)); alert('Backup importado. Clique em Salvar alterações.')}catch{alert('Arquivo inválido')}}; r.readAsText(f)}return <header className="topbar"><div><p className="eyebrow">Grupo Dom Bosco</p><h1>{title}</h1><span>{subtitle}</span></div><div className="topActions"><button className="ghost" onClick={()=>downloadJson(data)}>Exportar JSON</button><label className="ghost fileBtn">Importar JSON<input type="file" accept=".json" onChange={importJson}/></label><button className="primary" onClick={onSave}>Salvar alterações</button></div></header>}
